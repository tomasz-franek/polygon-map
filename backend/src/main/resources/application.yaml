spring:
  application:
    name: polygonMap

  data:
    mongodb:
      user: user
      password: password
      uri: mongodb://127.0.0.1:27017/polygonMap
server:
  compression:
    enabled: true
    mime-types: text/html,text/css,application/javascript,application/json
    min-response-size: 4096
